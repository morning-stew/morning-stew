# Morning Stew Configuration

# API Server (Railway sets PORT automatically)
PORT=3000

# X402 Payment Configuration
# The Solana wallet that receives payments
RECEIVER_ADDRESS=YourSolanaWalletAddressHere

# PayAI Facilitator URL (Solana-first, no API keys needed)
FACILITATOR_URL=https://facilitator.payai.network

# GitHub (optional, for higher API rate limits)
GITHUB_TOKEN=

# Twitter/X OAuth 1.0a (preferred for bot accounts — never expires)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# For content generation scripts (not needed for API server)
# OPENAI_API_KEY=
